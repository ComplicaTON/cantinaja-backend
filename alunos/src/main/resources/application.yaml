spring:
  application:
    name: alunos
  datasource:
    url: jdbc:h2:mem:alunosdb
    driver-class-name: org.h2.Driver
    username: sa
    password: teste
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8084
springdoc:
  swagger-ui:
    path: /swagger-ui.html
