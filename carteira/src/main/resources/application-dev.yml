spring:
  datasource:
    url: ${DATABASE_URL:jdbc:h2:mem:carteiradb}
    username: ${DATABASE_USERNAME:cantinaja}
    password: ${DATABASE_PASSWORD:cantinaja123}

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true