spring:
  application:
    name: pedidos
  datasource:
    url: jdbc:h2:mem:pedidosdb
    driver-class-name: org.h2.Driver
    username: cantinaja
    password: cantinaja123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8083
springdoc:
  swagger-ui:
    path: /swagger-ui.html
