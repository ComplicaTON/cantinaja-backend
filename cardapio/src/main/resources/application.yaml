spring:
  application:
    name: cardapio
  profiles:
    active: dev
  mvc:
    apiversion:
      use:
        path-segment: 1
      default: "v1"

server:
  port: 8081

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
